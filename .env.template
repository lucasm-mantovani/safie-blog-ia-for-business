# Credenciais do Blog IA for Business — copie este arquivo para .env e preencha os valores

# Anthropic — geração de artigos via Claude API
ANTHROPIC_API_KEY=

# GitHub — publicação automática via commit+push
GITHUB_TOKEN=
GITHUB_REPO=lucasm-mantovani/safie-blog-ia-for-business

# Apify — opcional, busca de notícias (fallback automático para RSS se vazio)
APIFY_TOKEN=
